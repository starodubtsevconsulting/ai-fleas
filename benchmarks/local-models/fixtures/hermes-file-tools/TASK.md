Use shell/filesystem tools to process INPUT.txt.

1. Create NORMALIZED.txt containing the unique input lines sorted alphabetically.
2. Create REPORT.txt with exactly these three lines, using actual computed values:
   input_lines=<number>
   unique_lines=<number>
   normalized_sha256=<sha256 of NORMALIZED.txt>
3. Verify both output files using shell commands before reporting completion.
