# Synology memory

Profile-scoped single-share wrapper for the reusable `synology` command. Bind one read-only memory share and only that
share's credentials. Use separate bindings when workflows must not receive each other's storage credentials.
