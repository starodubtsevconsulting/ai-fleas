# Optional local statements command configuration.
# Copy to statements.command.config if your reports root differs from the default.
INCORPORATED_ROOT="${HOME}/data/SynologyDrive/documents/incorparated"
# REPORTS_ROOT="${INCORPORATED_ROOT}/reports"
# STATEMENTS_OUTPUT_DIR="${PWD}/.ai/tmp/statements"
