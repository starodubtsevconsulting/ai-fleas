# Command configuration setup contract.
# Example files describe shape only. Copy them into the selected AI Profile, keep operational values ignored by Git,
# bind the path through that profile's commands[].config, and execute with AI_COMMAND_CONFIG_PATH resolved by the host.
# Never create or populate configuration beside a reusable command.
