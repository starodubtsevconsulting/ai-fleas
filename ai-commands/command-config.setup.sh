# This is common Interactive setup script
#  to be used by every command when command requires the config setup.
# if is creating new file it will copy the `[command-name].command.example.config` to `[command-name].command.config`
# example: `sms.command.example.config` to `sms.command.config`, asking user to fill the values.