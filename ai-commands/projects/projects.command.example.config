# Default values shipped with the repo.
# Copy this file to `projects.command.config` on first run; edit the copy instead.
PROJECTS_PATH="${HOME}/projects"
# Set `ORG` to match `projects-registry-<org>.yml`; leave it blank or set it to `example` to continue using the shared `projects-registry.example.yml`.
