Fixture pairs for AI compile quality checks.

- `*.source.md` = raw input text/post
- `*.expected.md` = target compiled speaker script shape

These are reference examples for prompt tuning (`rules/commands/tts/prompts/compile-post.prompt.md`).
They are intentionally kept even though the Python compiler/test path was removed.
