# GNOME Remote Desktop

This adapter enables Ubuntu GNOME's built-in RDP service for the current desktop session. Run it only through the
profile-aware parent command. Credentials are configured interactively with `grdctl rdp set-credentials` and remain in
GNOME's credential storage.
