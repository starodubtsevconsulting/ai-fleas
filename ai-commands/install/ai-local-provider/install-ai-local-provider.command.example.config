# Documentation-only example. Copy into the selected AI profile.
# Do not store SSH private keys or secrets here.

SSH_PORT=22

PROVIDER_RUNTIME=llama.cpp
PROVIDER_LISTEN_ADDRESS=127.0.0.1
PROVIDER_PORT=8080
PROVIDER_CONTEXT_SIZE=65536
PROVIDER_SERVICE_NAME=ai-local-provider

# Model values are intentionally not hard-coded by the command.
MODEL_SOURCE=
MODEL_FILE=
