#!/usr/bin/env bash

# normally it works with jetbrains but it can work in deattached mode too, in terminal.
curl -fsSL https://claude.ai/install.sh | bash

# it will install it to: ~/.local/bin/claude