# TEMPLATE ONLY: copy into the selected AI Profile; never populate this file in ai-commands.
# COMMITTED EXAMPLE ONLY: keep this file fictional and free of credentials.

# Example shape only:
# provider: slack
# integration: plugin-or-mcp
# account: example-workspace
# channels: [direct-message, channel, sms]
# default_destination: example-user
