#!/usr/bin/env bash
set -euo pipefail
# This file is intentionally unchanged by the per-agent AI-provider PR.
# Runtime consumption of the new mapping is implemented only after the mapping
# contract and focused tests are approved.
printf '%s\n' 'Use the main-branch Hermes lifecycle command while this draft mapping is under review.' >&2
exit 1
