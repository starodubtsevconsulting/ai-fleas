# EXAMPLE ONLY. The operational profile owns real provider IDs, endpoints, and credentials.
schema_version: workflow-agent-providers.v1
workflow_id: youtube

bindings:
  youtube-lyrics-script:
    provider: asus-gx10-model-provider
    model: gemma-writing-q8

  youtube-default-worker:
    # Intended cloud assignment when that provider and model are configured:
    # provider: openai-service
    # model: gpt-sol
    provider: asus-gx10-model-provider
    model: qwen-coder
