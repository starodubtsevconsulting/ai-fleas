# EXAMPLE ONLY: fictional host, paths, identities, and repository mapping.
nas:
  host: nas.example.invalid
  https_port: 5001
  certificate_sha256: REPLACE_WITH_VERIFIED_FINGERPRINT
  remote_access:
    mode: private-tunnel
    host: TODO_PRIVATE_TUNNEL_HOSTNAME
    quickconnect: false
shares:
  financial-memory:
    name: financial-memory
    account: agent-financial-memory
    access: read-only
    source: /absolute/path/to/existing/financial-memory
    mount: /Volumes/example-financial-memory
    usage: memory
    mutation: source-control
    repository:
      id: example-financial-memory
      branch: main
      subpath: records
      delivery: on-merge
      publisher_checkout: TODO_PUBLISHER_GIT_CHECKOUT
