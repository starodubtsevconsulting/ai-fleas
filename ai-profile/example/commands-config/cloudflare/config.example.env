# EXAMPLE ONLY. These are fictional values; tokens must come from the named environment variables.
# Each provider ID matches ai-profile/example/local-ai-providers.yml. Target files contain
# public/tunnel configuration and secret *references*; provider YAML contains the Hermes
# remote URL and Access service-token header references. No credential value is committed.
CLOUDFLARE_PROVIDER_TARGETS="asus-gx10-model-provider=targets/asus-gx10-model-provider.env,rtx-3080-ti-model-provider=targets/rtx-3080-ti-model-provider.env"
CLOUDFLARE_UI_LOG_LIMIT="10000"
