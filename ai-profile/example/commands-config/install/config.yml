# EXAMPLE ONLY: all identities and targets are fictional and non-operational.
defaults:
  ssh_port: 22
  preset: qwen3.5-9b-hermes
  provider_port: 8080
  storage_volume: /srv/ai-models

boxes:
  ai-lab-test:
    # Define this alias in the operator's private ~/.ssh/config.
    ssh_alias: ai-lab-test
    preset: qwen3.5-9b-hermes
    inventory: inventory/ai-lab-test.json
    remote_desktop:
      label: Example Linux Desktop
      host: 192.0.2.25
      username: example-user
      preferred_client: freerdp
      windows_app_compatible: false
      note: Example only; configure a private target in an operational profile.
