# Example workflow resources

Reusable workflow contracts remain in the sibling `ai-workflows/` catalog.
This folder contains profile-specific policies, templates, and overrides that
are activated only through explicit references in the work profile.
