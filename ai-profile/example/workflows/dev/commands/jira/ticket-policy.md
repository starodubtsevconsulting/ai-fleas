# Example ticket policy

This profile-specific example demonstrates policy referenced by the reusable
ticket-tracker and Jira commands. Replace it with the private policy for the
copied profile.

- Ticket summaries use `[project name]: title`.
- New tickets require a bounded description and human-time estimate.
- Lifecycle changes require provider readback evidence.
- Closure requires the workflow's acceptance evidence.
