# Copy this file to identity.config in the same directory and replace both
# TODO values. identity.config is ignored by Git and must remain local.
GIT_USER_NAME="TODO: your Git display name"
GIT_USER_EMAIL="TODO: your Git email"
