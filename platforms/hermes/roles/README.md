# Hermes role overlays

Hermes overlays may define profile naming and model/runtime tuning for a portable role. They may narrow, but never grant,
the authority defined by `ai-workflows/_common/roles/`. Profile-owned provider and model values are referenced rather than
copied here.
