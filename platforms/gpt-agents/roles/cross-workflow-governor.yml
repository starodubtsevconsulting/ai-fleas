schema_version: gpt-agents-role-overlay.v1
platform: gpt-agents
role: cross-workflow-governor
portable_role: ../../../ai-workflows/_common/roles/cross-workflow-governor.yml

realization:
  title: "Governor"
  model: gpt-5.6-sol
  reasoning: high

# Portable lifecycle, readiness, scope, human-facing semantics, authority, and
# intelligence requirement come from the profile binding and portable role.
# This overlay must still resolve initialization to a concrete GPT model and
# reasoning level; it cannot leave runtime model selection abstract.
