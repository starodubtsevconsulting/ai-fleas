schema_version: gpt-agents-role-overlay.v1
platform: gpt-agents
role: cross-workflow-governor
portable_role: ../../../ai-workflows/_common/roles/cross-workflow-governor.yml

realization:
  title: "Governor"
  model: gpt-5.6-sol
  reasoning: high
  lifecycle: persistent
  readiness_token: CROSS_WORKFLOW_GOVERNOR_READY

# This overlay realizes the portable role on GPT Agents only.
# It may tune model/runtime presentation but cannot grant additional authority.
