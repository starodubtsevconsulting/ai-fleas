schema_version: gpt-agents-role-overlay.v1
platform: gpt-agents
role: cross-workflow-governor
portable_role: ../../../ai-workflows/_common/roles/cross-workflow-governor.yml

realization:
  title: "Governor"
  model_selection: strongest-eligible-remote
  reasoning: high

# Portable lifecycle, readiness, scope, human-facing semantics, authority, and
# intelligence requirements come from the profile binding and portable role.
# This overlay maps those requirements to GPT/Codex-specific realization.
