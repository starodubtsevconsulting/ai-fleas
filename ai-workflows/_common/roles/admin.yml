schemaVersion: role.v1
extends: _role.yml

id: admin
contract: admin.md

scope:
  kind: workflow
  bindingOwner: workflow

humanFacing: true

lifecycle:
  default: persistent

fixed:
  canonicalRole: admin
