schemaVersion: role.v1
extends: _role.yml

id: ui-acceptance-tester
contract: ui-acceptance-tester.md

scope:
  kind: workflow
  bindingOwner: workflow

humanFacing: false

lifecycle:
  default: task-scoped

fixed:
  canonicalRole: ui-acceptance-tester
