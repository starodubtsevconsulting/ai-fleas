schemaVersion: role.v1
extends: _role.yml

id: manager
contract: manager.md

scope:
  kind: workflow
  bindingOwner: workflow

humanFacing: false

lifecycle:
  default: persistent

fixed:
  canonicalRole: manager
