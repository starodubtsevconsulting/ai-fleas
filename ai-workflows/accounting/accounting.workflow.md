# Accounting workflow

Use only for explicitly requested accounting work. Load only the commands and
rules needed for the request; do not load other workflow packages, session
state, or reporting instructions automatically.
