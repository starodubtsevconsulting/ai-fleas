# Alternate Dev MCP Discussion Fixture

This second fixture proves that the shared discussion lookup capability can be
mounted with a different provider without copying adapter or schema code.

It mentions alternate provider isolation and reusable command capability mounts.
