# Dev MCP Discussion Fixture

The dev workflow owns this injected fixture data. The reusable discussion
lookup command capability may read it only after the dev workflow composition
mounts that capability with this provider.

This file mentions capability boundary, command registry checks, workflow
owned data, and plugin isolation.
