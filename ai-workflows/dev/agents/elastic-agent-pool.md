# Elastic agent pool

Elastic capacity creates independent same-role instances; it never implies context continuity or authority inheritance.
Manager alone reconciles capacity under [Team policy](team.md). Each instance receives one non-overlapping assignment,
an exact identity binding, a verified return route, and its role readiness token before dispatch. Ambiguous lineage,
overlapping write scope, missing capacity, or incomplete identity is `BLOCKED`.
