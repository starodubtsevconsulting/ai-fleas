# Runtime permission envelope

The Team capability matrix and one valid packet define the maximum permitted effect. The selected platform may narrow
that envelope through its own sandbox, approval system, or host capabilities; it may never broaden it. Human-interactive
approval requested by an internal-only role is prohibited. Out-of-envelope work returns a blocker to the exact verified
caller without forwarding, substitution, or partial execution.
